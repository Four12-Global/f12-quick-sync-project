{
  "name": "f12-quick-sync",
  "version": "1.0.0",
  "description": "Airtable → WordPress Quick-Sync scripts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/Four12-Global/f12-quick-sync-project.git"
  },
  "scripts": {
    "build": "esbuild scripts-src/*.ts --bundle --format=iife --target=es6 --minify --outdir=scripts-dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.8"
  }
}